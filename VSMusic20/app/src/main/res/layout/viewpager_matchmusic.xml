<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@mipmap/bg_matchmusic">


    <TextView
        android:id="@+id/textView10"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:text="中心"
        android:visibility="invisible" />

    <ImageButton
        android:id="@+id/match_album"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@mipmap/button_album"
        android:layout_above="@+id/match_photo"
        android:layout_centerHorizontal="true" />

    <ImageButton
        android:id="@+id/match_photo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@mipmap/button_photo"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/match_album" />
</RelativeLayout>